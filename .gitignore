# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
# Per-demo dev HTML entries from `npm run dev` (isolated so concurrent dev
# servers don't clobber a shared index.html).
/.dev
# Raw sampler scratch at repo root only — curated finds live in
# demos/flat-samples/samples/ and ARE tracked. Anchored with a leading
# slash so it doesn't also match that nested directory.
/samples

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
package-lock.json
