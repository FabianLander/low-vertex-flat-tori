# geometry/ — pure ℝ²/ℝ³ kernels

Torus-blind geometric primitives reused across the search layer: 2D vectors,
point/segment/triangle squared-distances, the triangle–triangle intersection chord,
plane curves. No `Triangulation`, no configuration-space knowledge.

*(Empty for now. Populated during the refactor by rewriting `math/distance.ts`,
`math/intersectionChord.ts`, `math/plane/*` into this layer.)*
