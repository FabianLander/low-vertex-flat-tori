# functions/ — differentiable maps on configuration space

The smooth maps C → ℝᵏ (value + derivative) that everything else is built from: the
cone-angle deficit (→ flatness), the modulus τ (→ modulus submanifolds), the embedding
margin, the repulsion/barrier energies. These are *measured* in many roles —
constraints, certificates, diagnostics, plots — which is why they live on their own,
separate from the loci they define.

*(Empty for now. Populated during the refactor by rewriting `math/angles.ts`,
`math/energies/*`, the τ map from `topology/develop.ts`, etc. into this layer.)*
